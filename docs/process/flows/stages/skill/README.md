# 技能提取

## 文件角色

当相同错误模式重复出现时，将教训提升为 `docs/skills/` 下的可复用技能。

## 进入条件

log 阶段检测到重复模式 ≥2 次。否则跳过。

## 执行步骤

将重复出现的过程教训提升为 `docs/skills/` 下的可复用技能。

## 产出物

| 产出 | 路径 |
|------|------|
| 新技能 | `docs/skills/<分类>/<名称>/SKILL.md` |

## 完成证明

`Test-Path` 确认新 skill 文件存在。

## 退出路由

流程结束。
