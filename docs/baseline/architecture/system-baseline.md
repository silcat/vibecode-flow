# System Baseline

## 用途

记录 `<项目名称>` 的运行时形状和技术基线。

此文件描述系统"长什么样"。

---

## 架构模式

<!-- 三选一，填完后删掉另外两条和本注释 -->

**单体应用**
<!-- **微服务**（本项目含 N 个独立部署服务） -->
<!-- **混合**（单体主应用 + M 个独立服务） -->


## 技术栈

| 层 | 选型 | 版本 |
|----|------|------|
| `<后端框架>` | `<选型>` | `<版本>` |
| `<数据库>` | `<选型>` | `<版本>` |
| `<缓存>` | `<选型>` | `<版本>` |
| `<消息队列>` | `<选型>` | `<版本>` |

## 外部平台

| 平台 | 用途 | 调研 | 技能 |
|------|------|------|------|
| `<平台名>` | `<用途>` | `docs/work/<branch>/research.md` | `docs/skills/tech/tech-<名>/SKILL.md` 或 — |

> 引入新外部平台时，先经阶段 ④ 调研产出 research.md，批准后在此注册。如何使用该平台见对应 tech 技能。

## 验证命令

| 用途 | 命令 |
|------|------|
| 安装依赖 | `<命令>` |
| 编译检查 | `<命令>` |
| 单元测试 | `<命令>` |
| 集成测试 | `<命令>` |
| 本地运行 | `<命令>` |

## 更新触发条件

以下情况才需要改本文件：

- 技术栈版本升级
- 新增/移除外部平台依赖
- 验证命令变更
