# 开发日志 - 2026-06-24

## 2026-06-24

- 新增 `docs/baseline/standards/layering.md`：分层架构规范，含完整的目录结构、依赖规则、各层详解、信号驱动演进路径、共享逻辑处理和同步互调处理
- 更新 `docs/baseline/standards/module-structure.md`：目录骨架从 `service/impl/` 改为 `bo/` + `repository/` + `domain/` + `gateway/` 平级独立结构；必选目录新增 `bo/`，可选目录新增 `domain/`、`gateway/`
- 更新 `docs/baseline/context/conventions.md`：规范路由表追加分层架构条目
- 更新 `docs/reference/spring/coding-conventions.md`：分层架构改为 Controller → BO → Repository/Gateway → Mapper/Feign/SDK；追加铁规（BO 之间禁止互调）和信号驱动演进表
